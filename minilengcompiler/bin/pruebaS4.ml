%------------------------------------------------------
programa pruebaS4;

lolaso
%------------------------------------------------------
principio
 %leer(); % error, continua compilacion
 %leer(p1, p2);
 %leer(p1);
 %escribir(); % error, continua compilacion
 %escribir(1+2+3*4+5); % con DEBUG_PARSER = TRUE se puede ver que tiene en cuenta las prioridades
 
fin

